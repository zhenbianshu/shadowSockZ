group 'com.zhenbianshu'

dependencies {
    compile(project(':common'))
}

jar {
    baseName = 'client'

    manifest {
        attributes "Manifest-Version": 1.0,
                'Main-Class': 'com.zhenbianshu.shadowsockz.run.Bootstrap'
    }
}